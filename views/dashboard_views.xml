<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Client action that loads the OWL PtiDashboard component -->
    <record id="pti_dashboard_action" model="ir.actions.client">
        <field name="name">PTI Dashboard</field>
        <field name="tag">pti_ar.dashboard</field>
        <field name="target">current</field>
    </record>

</odoo>
